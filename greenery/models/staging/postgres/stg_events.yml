version: 2

models:
  - name: stg_events
    description: List of events
    columns:
      - name: event_id
        description: UUI for each unique event
        tests: 
          - not_null
          - unique
      - name: session_id
      - name: user_id
      - name: page_url
      - name: created_at
      - name: event_type
      - name: order_id
      - name: product_id