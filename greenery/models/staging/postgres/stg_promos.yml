version: 2

models:
  - name: stg_promos
    description: table of promotions
    columns:
      - name: discount
      - name: promo_id
        description: UUID for promotions
        tests: 
          - not_null
          - unique
      - name: status